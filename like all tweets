#1 copy the code below
#2 go over to a user's profile https://twitter.com/[usernamme]
#3 open inspect element. (ctrl shift + i) or click the 3 dots at the top of chrome -> more tools -> developer tools
#4 click on console tab and paste the code below in and hit enter
~ if it doesnt unlike all of them just refresh and do it again.

you can only like 300 a day using this, also be careful if you mass like without a phone number because you will most likely get suspended fast
--

setInterval(() => {
  for (const d of document.querySelectorAll('div[data-testid="like"]')) {
    d.click()
  }
  window.scrollTo(0, document.body.scrollHeight)
}, 1000) -- can change just no point really
